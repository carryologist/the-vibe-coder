# Required — Admin authentication
ADMIN_PASSWORD=
SESSION_SECRET=          # Random string for JWT signing (e.g., openssl rand -hex 32)

# Required — Content storage (GitHub)
GITHUB_TOKEN=            # Personal access token with repo contents write access
GITHUB_REPO=             # Format: owner/repo (e.g., carryologist/the-vibe-coder)

# Required — AI blog generation (Anthropic)
ANTHROPIC_API_KEY=       # Claude API key from console.anthropic.com

# Required — Analytics (Upstash Redis)
KV_REST_API_URL=         # Upstash Redis REST URL
KV_REST_API_TOKEN=       # Upstash Redis REST token

# Required — MCP server
MCP_API_TOKEN=           # Bearer auth for MCP server endpoint
APP_BASE_URL=            # Base URL for CORS origin (e.g., https://vibescoder.dev)

# Required — Deployment
VERCEL_DEPLOY_HOOK=      # Webhook URL to trigger Vercel deploys

# Optional — Integrations
SLACK_SIGNING_SECRET=    # Slack request verification secret
DEVTO_API_KEY=           # Dev.to API key for cross-posting
CODER_API_TOKEN=         # Personal Coder-Session-Token for coder.vibescoder.dev,
                         # used by /admin/todo's "Launch Agent" button to call
                         # the Coder Agents Chats API (POST /api/experimental/chats)
